@model Buoi6.Models.Category

@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1 class="mb-4">Xóa danh mục</h1>

<div class="alert alert-warning" role="alert">
    <h3>Bạn có chắc chắn muốn xóa danh mục này không?</h3>
    <p>Lưu ý: Hành động này sẽ xóa toàn bộ sản phẩm thuộc danh mục này. Hành động này không thể hoàn tác!</p>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <h4 class="card-title">Danh mục</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-3">
                @Html.DisplayNameFor(model => model.Name)
            </dt>
            <dd class="col-sm-9">
                @Html.DisplayFor(model => model.Name)
            </dd>
        </dl>

        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button type="submit" class="btn btn-danger"><i class="bi bi-trash"></i> Xóa</button>
            <a asp-action="Index" class="btn btn-secondary ms-2"><i class="bi bi-arrow-left"></i> Quay lại danh sách</a>
        </form>
    </div>
</div>